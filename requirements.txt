flask>=3.0
pillow>=10.0   # required on the Pi for motion detection (and the dev mock camera)

# System packages (not pip):
#   ffmpeg  — required for MP4 recordings; without it, recordings stay MJPEG
